@model bool
@{
    var o = new System.Collections.Generic.Dictionary<string, object>();

    if (ViewData.ModelMetadata.IsReadOnly)
    {
        o.Add("class", "disabled");
        o.Add("disabled", "disabled");
        o.Add("readonly", "readonly");
    }

    @Html.CheckBox("", Model, o);
}